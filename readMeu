.\.venv\Scripts\activate
source .venv/bin/activate
py manage.py runserver 8001

pip install django python-dotenv psycopg2-binary
pip install openpyxl xhtml2pdf
pip install djangorestframework

python manage.py collectstatic --noinput
sudo systemctl restart gunicorn-intranet


python manage.py shell -c "
from django.contrib.auth.models import Group
for nome in ['admin','manager','financeiro','incorporadora']:
    g, created = Group.objects.get_or_create(name=nome)
    print(('Criado' if created else 'Já existe') + ': ' + nome)
"
